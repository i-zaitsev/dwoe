# dwoe-agent:c is an image to work with C (C23) using Claude Code.
# Build: docker build -f docker/Dockerfile.claude-c -t dwoe-agent:c docker/
# Uses Debian Trixie for GCC 14 which has full C23 support (-std=c23).
FROM debian:trixie-slim

ARG USER_ID=9999
ARG GROUP_ID=9999

RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    ca-certificates \
    curl \
    jq \
    gcc \
    libc6-dev \
    make \
    cmake \
    pkg-config \
    gdb \
    valgrind \
    && rm -rf /var/lib/apt/lists/*

RUN groupadd -g ${GROUP_ID} agent && \
    useradd -m -u ${USER_ID} -g agent -s /bin/bash agent

RUN mkdir -p /workspace /logs && \
    chown -R agent:agent /workspace /logs

USER agent
WORKDIR /workspace

RUN curl -fsSL https://claude.ai/install.sh | bash
ENV PATH="/home/agent/.local/bin:${PATH}"

RUN mkdir -p /home/agent/.claude

COPY entrypoint.sh /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]
