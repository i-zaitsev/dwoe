Add a `greet` package with a `Hello(name string) string` function
that returns "Hello, {name}!".

Add tests in `greet/greet_test.go`. Run `go test ./...` and make
sure everything passes. Commit the result.
