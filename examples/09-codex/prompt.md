Add a `greet` function in `greet.py` that takes a `name` string and
returns "Hello, {name}!".

Add tests in `test_greet.py`. Run `python -m pytest` and make sure
everything passes. Commit the result.
