Add `features/greet.go` with a `Greet(name string) string` function
that returns "Hello, {name}!".

Add `features/greet_test.go` with tests. Run `go test ./features/...`
and make sure all tests pass.
