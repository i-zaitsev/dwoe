Add `features/math.go` with two functions:
- `Add(a, b int) int` — returns a + b
- `Multiply(a, b int) int` — returns a * b

Add `features/math_test.go` with tests. Run `go test ./features/...`
and make sure all tests pass.
