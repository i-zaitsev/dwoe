Add `features/reverse.go` with a `Reverse(s string) string` function
that reverses a string (handle multi-byte characters correctly using
runes).

Add `features/reverse_test.go` with tests. Run `go test ./features/...`
and make sure all tests pass.
